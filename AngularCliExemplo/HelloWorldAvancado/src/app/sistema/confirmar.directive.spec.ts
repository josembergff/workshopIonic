import { ConfirmarDirective } from './confirmar.directive';

describe('ConfirmarDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmarDirective();
    expect(directive).toBeTruthy();
  });
});
